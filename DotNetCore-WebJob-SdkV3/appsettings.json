{
  // used for local development only; production values should be overriden via environment variables
  "ConnectionStrings": {
    // values must always be set, otherwise webjob SDK crashes with ArgumentNullException
    "AzureWebJobsDashboard": "UseDevelopmentStorage=true",
    "AzureWebJobsStorage": "UseDevelopmentStorage=true"
  },
  // empty to keep AI events local to Visual Studio
  "APPINSIGHTS_INSTRUMENTATIONKEY":  ""
}
